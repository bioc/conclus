Changes in version 0.99.336 (2021-04-08)
+ Submission to Bioconductor

+ NAMESPACE
 o Imported BiocFileCache package and R_user_dir() of tools package
 o Exported new function conclusCacheClear()

+ DESCRIPTION
 o removed LazyData: true.

+ DataFormatting.R :
  o added a caching system for retrieveFromGEO()
  o created conclusCacheClear() to delete the cache
  o updated documentation
  
+ loadDataset.R : 
  o updated documentation
  o simplified nested "if" in loadDataset.R
  
+ methods-normalization.R :
    o Modified the use of getBM to retrieve only genes of the count matrix (instead of the all database)
    
+ test_setters.R/test_getters.R :
  o modified documentation for loadDataOrMatrix()
  o simplified nested "if" in loadDataset.R
  
+ test_loadData.R :
 o Adapted the unit tests to the new format of coldata and rowdata
 
+ test_scRNAseq-methods.R:
 o Changed the experiment name to "Light_Experience"
 o Used tempdir() for output directory

+ inst
 o Added inst/script to generate data on inst/extdata
 o New data generated
 
+ vignette:
 o Used tempdir() for output directory
 o Specified other parameters in the first example of runCONCLUS for the very small dataset
 o Replaced old paths by new ones